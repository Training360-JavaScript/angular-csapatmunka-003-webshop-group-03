import { TextsorterPipe } from './textsorter.pipe';

describe('TextsorterPipe', () => {
  it('create an instance', () => {
    const pipe = new TextsorterPipe();
    expect(pipe).toBeTruthy();
  });
});
