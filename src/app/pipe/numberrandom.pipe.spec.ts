import { NumberrandomPipe } from './numberrandom.pipe';

describe('NumberrandomPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberrandomPipe();
    expect(pipe).toBeTruthy();
  });
});
